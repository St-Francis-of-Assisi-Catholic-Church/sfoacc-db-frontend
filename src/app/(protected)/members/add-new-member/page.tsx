import { metaObject } from "@/config/site.config";
import React from "react";

export const metadata = {
  ...metaObject("Members | Add new member"),
};

function page() {
  return <>welcome to add new member</>;
}

export default page;
