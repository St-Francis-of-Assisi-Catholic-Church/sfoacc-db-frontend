import { metaObject } from "@/config/site.config";
import React from "react";

export const metadata = {
  ...metaObject("Societies | Home"),
};

function page() {
  return <div>Societies Home</div>;
}

export default page;
